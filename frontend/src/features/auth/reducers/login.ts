import type { AppThunk } from "@/app/store";
import type User from "@/data/model/user";
// import { createAsyncThunk } from "@reduxjs/toolkit";
//
// export const submitLogin = createAsyncThunk(user: User) {
//   return (dispatch, getState) => {
//
// 		};
// };
